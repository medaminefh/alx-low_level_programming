#include "main.h"
/**
 * add - This will convert ascii number into char
(* a blank line
 * Description: by placing %c instead of %d, the number will be converted)?
(* section header: Section description)*
 * @a: is of type int
 * @b: is of type int
 * Return: just return 0 for ok
 */

int add(int a, int b)
{
int result = a + b;
return (result);
}
