#include "lists.h"
#include <stdio.h>

/**
 * print_listint - check the code
 * @h: is a list to be printed
 * Return: Always 0.
 */

size_t print_listint(const listint_t *h)
{
int len = 0;
while (h)
{
printf("%d\n", h->n);
len++;
h = h->next;
}
return (len);
}

