#include "main.h"

/**
 * mul - This will convert ascii number into char
(* a blank line
 * Description: by placing %c instead of %d, the number will be converted)?
(* section header: Section description)*
 * @a: is an int
 * @b: is an int
 * Return: just return 0 for ok
 */
int mul(int a, int b)
{
return  (a * b);
}
